# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0

# --- Agent orchestration (LangGraph 1.0) ---
langgraph>=1.0
langchain-google-genai>=2.0
langchain-core>=1.0
langchain-text-splitters>=1.0

# --- RAG ---
chromadb>=1.0
# 6.16.1 미만에 PDF 파싱 DoS 권고 12건 (무한 루프·메모리 폭증). 하한을 올리면 배포 빌드의
# pip 설치 캐시도 무효화돼, 첫 빌드 때 받은 옛 버전이 계속 쓰이는 일을 막는다.
pypdf>=6.16.1
networkx>=3.0
